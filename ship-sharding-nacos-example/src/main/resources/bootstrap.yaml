spring:
  application:
    name: ship-sharding-example
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yaml
server:
  port: 9000